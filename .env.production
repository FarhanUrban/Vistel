# Production builds always use live Firebase (overrides .env mock default)
VITE_USE_MOCK_SERVICES=false

# Store uploaded documents in the browser (IndexedDB). Free — no Firebase Storage plan needed.
# Set to "firebase" only after enabling Firebase Storage on a Blaze plan.
VITE_DOCUMENT_STORAGE=local
