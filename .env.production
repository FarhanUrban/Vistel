# Production builds always use live Firebase (overrides .env mock default)
VITE_USE_MOCK_SERVICES=false

# Document uploads + applications stored in browser localStorage (pretend upload).
# Firebase Auth still live. No Firebase Storage or Firestore for documents.
VITE_DOCUMENT_STORAGE=local
