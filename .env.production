# Production builds always use live Firebase (overrides .env mock default)
VITE_USE_MOCK_SERVICES=false

# Document file bytes: "local" (IndexedDB) or "firebase" (paid Storage plan).
# Metadata always goes to Firestore — Auth, applications, interviews, and document records.
VITE_DOCUMENT_STORAGE=local
