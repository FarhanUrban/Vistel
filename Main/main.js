/**
 * Legacy placeholder — Firebase is initialized in the Vue app via env vars.
 *
 * Config lives in `.env` (gitignored). See:
 *   - src/services/api.ts
 *   - .env.example
 *
 * Do not add API keys or secrets to this file.
 */
